import 'package:flutter/material.dart';

class DoseActivityScreen extends StatelessWidget {
  const DoseActivityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dose Activity Screen'),
    );
  }
}
